services:
  db:
    image: postgres:16
    environment:
      POSTGRES_USER: supmeal
      POSTGRES_PASSWORD: supmeal
      POSTGRES_DB: supmeal
    volumes:
      - db_data:/var/lib/postgresql/data
    ports:
      - "5432:5432"

volumes:
  db_data:
